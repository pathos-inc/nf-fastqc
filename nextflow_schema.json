{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/./master/nextflow_schema.json",
    "title": "MultiQC pipeline parameters",
    "description": "Fastp and MultiQC only pipeline",
    "type": "object",
    "definitions": {
      "input_output": {
        "title": "input/output",
        "type": "object",
        "description": "",
        "default": "",
        "properties": {
          "readsdir": {
            "type": "string",
            "default": "None",
            "description": "Directory containing reads files to be QC'd",
            "format": "directory-path"
          }
        },
        "help_text": "",
        "required": ["readsdir"]
      }
    },
    "allOf": [
      {
        "$ref": "#/definitions/input_output"
      }
    ]
  }
  